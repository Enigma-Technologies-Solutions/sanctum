# Build output — generated by `pnpm build` / `pnpm tauri build`
dist/
# dist-isolation/ is NOT ignored — it contains static source files
# (the Tauri Isolation Pattern IPC hook) that are required at build time.

# Tauri build
src-tauri/target/
src-tauri/gen/

# Node
node_modules/

# Env
.env
.env.*

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
